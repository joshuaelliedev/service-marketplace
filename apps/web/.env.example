NODE_ENV=development
# Optional: browser calls /api/* ; Next proxies to this Nest URL (see next.config.ts).
# API_UPSTREAM=http://127.0.0.1:3001
# Optional: only for server-side fetches / non-browser tools
# NEXT_PUBLIC_API_URL=http://127.0.0.1:3001
NEXT_PUBLIC_APP_NAME=Service marketplace
