NODE_ENV=development
# Browser calls this URL directly when set (recommended for Vercel + Render API).
NEXT_PUBLIC_API_URL=http://127.0.0.1:3001
# Optional local proxy fallback when NEXT_PUBLIC_API_URL is unset:
# API_UPSTREAM=http://127.0.0.1:3001
NEXT_PUBLIC_APP_NAME=Service marketplace (admin)